name: tools
description: MongoDB tools as a CLI plugin
version: $VERSION
github:
    owner: $GITHUB_REPOSITORY_OWNER
    name: $GITHUB_REPOSITORY_NAME
binary: binary
commands: 
    tools: 
        description: Entry point for the plugin