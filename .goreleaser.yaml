# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
project_name: atlas-cli-plugin-tools

version: 2

builds:
  - 
    id: "atlas-cli-plugin-tools"
    main: ./cmd/plugin/main.go
    binary: ./binary

archives:
  - files:
      - src: "./manifest.yml"
        dst: ./manifest.yml
